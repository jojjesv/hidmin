import ScoreEntry from "./ScoreEntry";

/**
 * Represents a single game with score entries.
 * @author Johan Svensson
 */
export default class {
  id: any;
  title: string;
  entries: ScoreEntry[];
  secret: string;
}